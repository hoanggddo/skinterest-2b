# Linux/Windows
tensorflow==2.20.0; not (platform_system == "Darwin" and platform_machine == "arm64")

# macOS Apple Silicon
tensorflow-macos==2.16.2; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal==1.1.0;  platform_system == "Darwin" and platform_machine == "arm64"

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
opencv-python>=4.8
tqdm>=4.66
pyyaml>=6.0
pillow>=10.0
streamlit>=1.36
